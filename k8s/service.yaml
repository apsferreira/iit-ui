apiVersion: v1
kind: Service
metadata:
  name: storybook
  namespace: storybook
  labels:
    app: storybook
spec:
  selector:
    app: storybook
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
